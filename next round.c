#include<stdio.h>
int main()
{
    int n,k,i,c=0;
    int arr[n];
    scanf("%d %d",&n,&k);

    while(n--) {
            scanf("%d",&arr[i]);
    for(i=1;i<=n;i++)

    if(arr[i]>=arr[k] && arr[i]>0)

        c++;
    }
    printf("%d",c);

}
